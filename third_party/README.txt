I placed the mpi-chi2 implementation that I used into this directory.
To compile this code, you should be able to just run 'make' in the mpi-chi2 directory.
However, the MATLABDIR variable in the Makefile may need to be set to the value of 'matlabroot', which you can run from the MATLAB console.
